# Phase 03: Dashboard & Config Update

Status: ⬜ Pending
Dependencies: Phase 02

## Objective

Tích hợp hiệu ứng mới vào Dashboard quản trị và file cấu hình server.

## Requirements

### Functional

- [ ] Cập nhật `dist/config.json` thêm preset `victory`.
- [ ] Chỉnh sửa `index.html` (Dashboard) để thêm option "30/04 Anniversary" vào dropdown chọn hiệu ứng.
- [ ] Thêm các slider điều chỉnh riêng cho `victory`: Tốc độ pháo hoa, Số lượng chim bồ câu.

## Implementation Steps

1. [ ] Cập nhật file cấu hình mặc định.
2. [ ] Thêm logic UI vào Dashboard UI (Glassmorphism style).
3. [ ] Viết hàm `applyPreset('victory')` trong code Dashboard.

## Test Criteria

- [ ] Chỉnh sửa trên Dashboard cập nhật đúng hiệu ứng Live Preview.
- [ ] Nút "Copy Embed Code" tạo ra mã nhúng đúng cho hiệu ứng 30/04.
